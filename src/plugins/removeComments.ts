import { PluginDef } from ".";

const removeComments: PluginDef = {
  id: "removeComments",
  name: "Remove comments",
  description: "Removes comments",
  params: [],
};

export default removeComments;
