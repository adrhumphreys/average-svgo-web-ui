import { PluginDef } from ".";

const removeEditorsNSData: PluginDef = {
  id: "removeEditorsNSData",
  name: "Remove editors NS data",
  description: "Removes editors namespaces, elements and attributes",
  params: [],
};

export default removeEditorsNSData;
