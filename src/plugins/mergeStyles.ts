import { PluginDef } from ".";

const mergeStyles: PluginDef = {
  id: "mergeStyles",
  name: "Merge styles",
  description: "Merge multiple style elements into one",
  params: [],
};

export default mergeStyles;
