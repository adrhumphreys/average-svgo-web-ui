import { PluginDef } from ".";

const removeDoctype: PluginDef = {
  id: "removeDoctype",
  name: "Remove Doctype",
  description: "Removes doctype declaration",
  params: [],
};

export default removeDoctype;
