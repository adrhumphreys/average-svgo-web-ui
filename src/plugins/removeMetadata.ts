import { PluginDef } from ".";

const removeMetadata: PluginDef = {
  id: "removeMetadata",
  name: "Remove metadata",
  description: "Removes <metadata>",
  params: [],
};

export default removeMetadata;
