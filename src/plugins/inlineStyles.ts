const minifyStyles = {
  id: "inlineStyles",
  name: "Inline styles",
  params: [
    {
      id: "onlyMatchedOnce",
      name: "Only matched once (enable to disable all styles)",
      type: "boolean",
    },
  ],
};

export default minifyStyles;
